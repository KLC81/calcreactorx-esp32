# 2026-05-31 工作记录

## 今日完成

- 通过 USB 串口识别 ESP32 开发板，确认芯片为 ESP32-D0WD-V3。
- 在重写前完整读取并保留原始 4 MB Flash 镜像。
- 根据现有硬件重新创建 PlatformIO 工程。
- 使用 ADS1115 读取一路钙反应 pH 模块模拟电压。
- 为 PH-4502C 与 3.3 V ADS1115 设计 `10kΩ + 18kΩ` 分压接法。
- 使用 GPIO26 控制 5 V 单路继电器。现场测试后确认当前模块为高电平触发。
- 实现 pH 两点校准：pH 7.00 与 pH 4.00。
- 实现自动控制阈值设置、手动开关阀门和断电保存。
- 加入中值滤波、指数平滑、异常跳变过滤和继电器动作后的稳定等待。
- 实现适合电脑与手机操作的本地网页控制面板。
- 后续增加 ESP32 自带热点入口，手机可连接 `AquaPH-Controller` 后访问
  `http://192.168.4.1`。
- 将固件烧录到 ESP32，并确认设备可以连接局域网。

## 当前运行状态

- ESP32 已连接 Wi-Fi。
- 网页控制面板可通过设备 IP 访问。
- 继电器上电默认关闭。
- 自动控制上电默认关闭，需要在网页中主动启用。
- ADS1115、PH-4502C 和继电器尚未实际连接，因此网页会显示 ADS1115 未检测到。

## 接下来

1. 按照根目录 `README.md` 连接 ADS1115、PH-4502C、分压电阻和继电器低压侧。
2. 保持 220 V 市电断开，先验证网页显示的电压是否稳定。
3. 使用 pH 7.00 与 pH 4.00 标准液完成两点校准。
4. 测试继电器低压控制。
5. 低压测试完成后，再由熟悉市电布线的人连接 220 V 电磁阀。
6. 购买触摸屏后，在当前控制逻辑上增加本地触控界面。

## 本机保留但不上传

以下内容可能包含 Wi-Fi 信息或设备现场数据，仅保存在本机：

- `include/secrets.h`
- `firmware_backup_*/`
- `release/*.bin`
- `release/SHA256SUMS.txt`
